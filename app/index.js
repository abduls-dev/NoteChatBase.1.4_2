import { StyleSheet, Text, View, Image } from "react-native";
import React from "react";
import HomeScreen from "./screens/ChatScreens/HomeScreen";

const index = () => {
	return <HomeScreen />;
};

export default index;

const styles = StyleSheet.create({});
